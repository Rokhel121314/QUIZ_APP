import {View, Text, SafeAreaView} from 'react-native';
import React from 'react';

const HomeScreen = () => {
  return (
    <SafeAreaView className="flex-1 bg-blue-400 justify-center items-center">
      <Text className="text-gray-50">HomeScreen</Text>
    </SafeAreaView>
  );
};

export default HomeScreen;
